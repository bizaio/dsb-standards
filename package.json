{
  "name": "dsb-standards",
  "version": "1.3.0",
  "dependencies": {
    "@redocly/openapi-cli": "^0.12.16",
    "api-spec-converter": "git+https://github.com/LucyBot-Inc/api-spec-converter.git",
    "deploy-to-gh-pages": "^1.3.7",
    "redoc": "^2.0.0-rc.33",
    "redoc-cli": "^0.9.8"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs",
    "build": "openapi bundle --ext json -o dist && openapi bundle --ext yaml -o dist && api-spec-converter dist/openapi.json --from=openapi_3 --to=swagger_2 > dist/swagger2.json",
    "deploy": "npm run build && cp dist/openapi.* web && deploy-to-gh-pages --update web",
    "test": "openapi validate"
  }
}
