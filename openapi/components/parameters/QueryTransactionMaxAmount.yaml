description: >-
  Filter transactions to only transactions with amounts less than or equal to
  than this amount
in: query
name: max-amount
required: false
schema:
  type: string
x-cds-type: AmountString
