description: >-
  Filters accounts based on whether they are owned by the authorised customer. 
  True for owned accounts, false for unowned accounts and absent for all
  accounts
in: query
name: is-owned
required: false
schema:
  type: boolean
x-cds-type: Boolean
