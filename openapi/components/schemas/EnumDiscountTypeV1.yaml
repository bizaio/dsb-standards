description: >-
  The type of discount. See the next section for an overview of valid values
  and their meaning
enum:
  - BALANCE
  - DEPOSITS
  - PAYMENTS
  - FEE_CAP
  - ELIGIBILITY_ONLY
type: string
example: BALANCE
